---
modsecurity:
  prefix:
    # TODO: FreeBSD path
    nginx_modules: "/usr/share/nginx/modules"
  dependencies:
    - libgeoip-dev
    - libcurl4-openssl-dev
    - liblua5.2-dev
    - libyajl-dev
    - libxml2-dev
    - libpcre3-dev
    - libpcre++-dev
    - zlib1g-dev
    - ssdeep
    - libfuzzy-dev
    - git
  build_dependencies:
    - build-essential
  packages_keep: []
